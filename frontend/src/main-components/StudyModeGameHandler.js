let rounds = 0;
let Quiz = [];
/*
function startQuiz(folderKey) {
    // TODO(esaracay): Make call to the study Servelet with folderKey
    Quiz = exampleQuiz;
    rounds = Quiz.length;
    return rounds;
}

export { startQuiz };
*/
